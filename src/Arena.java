public class Arena {

    // Static Methods

    // Players and their loot, receive damage by their weapon attacks
    public static void attack(Player p1, Player p2) {

    }

    // Players attack each other until ones of their healths depletes.
    // Returns 0 or 1 depending on the winner (p1 or p2 respectively)
    public static int fight(Player p1, Player p2) {
        return 0;
    }
}
